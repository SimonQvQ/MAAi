services:
  maai:
    build:
      context: .
      dockerfile: Dockerfile.mwu
    container_name: maai
    restart: unless-stopped
    ports:
      - "17171:17171"   # MAAiAgent 通道（iPhone -> Docker）
      - "8080:8080"     # MWU Web 控制台
    environment:
      - MAAI_WEB_PORT=8080
